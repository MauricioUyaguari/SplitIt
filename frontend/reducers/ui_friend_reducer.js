import merge from 'lodash/merge';






const uiFriendReducer = (state = {}, action) => {

  Object.freeze(state);

  switch (action.type) {
    default:
    return state;
  }
};



export default uiFriendReducer;
